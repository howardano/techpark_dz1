on: push

jobs:
  analysis:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: apt install lcov

  build:
    runs-on: ubuntu-latest
    needs: [analysis]
    steps:
    - uses: actions/checkout@v2 
     - run: apt install lcov
     
  test:
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - uses: actions/checkout@v2 
      - run: apt install lcov